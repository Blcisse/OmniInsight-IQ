# Core Backend & API
fastapi==0.116.1
uvicorn==0.38.0
starlette==0.47.3
orjson==3.11.4
pydantic==2.11.7
python-dotenv==1.1.1
pydantic-settings==2.2.1

# Database
sqlalchemy==2.0.43

# HTTP / Utility
httpx==0.28.1
requests==2.32.4
tenacity==9.1.2
pyyaml==6.0.2

# Data / Analytics (lightweight)
pandas==2.2.3
numpy==2.3.2
scipy==1.16.1
scikit-learn==1.7.1
joblib==1.5.1

# Caching (optional, but low overhead if you already use it)
redis==7.0.1

# Parsing / Docs (optional; keep only if used)
beautifulsoup4==4.13.4
pypdf==6.0.0

# Logging / Observability (keep only what you actively use)
structlog==25.5.0
coloredlogs==15.0.1

